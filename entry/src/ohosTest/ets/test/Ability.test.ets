import { describe, it, expect } from '@ohos/hypium';

export default function abilityTest() {
  describe('HmSSHAbility', () => {
    it('placeholder', 0, () => {
      expect(true).assertTrue();
    });
  });
}
